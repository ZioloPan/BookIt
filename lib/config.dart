class Config {
  /// Base URL for the API.
  /// Use `10.0.2.2` to access the PC's localhost from the Android emulator.
  /// Change for physical devices or production environments.
  static const String baseUrl = "http://10.0.2.2:3000";
}